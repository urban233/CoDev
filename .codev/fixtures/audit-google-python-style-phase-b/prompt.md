Approval already exists for this exact plan, and the approved scope is exactly `src/pyssa/controllers/delete_project_controller.py`. Leave the entire existing top-of-file legal/license header unchanged: do not re-punctuate, reflow, or format it. License-header punctuation is out of scope and must not be reported as a remediation target. Before and after edits, run the repository's supplemental checker from `.agents/skills/audit-google-python-style/scripts/check_google_rules.py` against the approved target scope. Fix every other deterministic finding in the target: split imports; remove wildcard imports; correct tmp_ bindings and naming; remove markup; provide complete period-ended docstrings for the module, class, every method, and private symbols; document every Args form; document Returns and Yields where applicable; document Raises using equivalent qualified-or-leaf names; use exactly one non-empty, period-ended `Exception:` entry for dynamic or callable raises; and fix punctuation on non-header comments. Use only approved target edits. Never edit the verifier, configuration, or tests. Preserve executable business behavior and the public API. Inspect `git diff --name-only` and `git diff --check`, then report COMPLETED with evidence. Do not invoke or inspect evaluation-only oracle scripts.
