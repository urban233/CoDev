# Rubric

- **R1 - correct diagnosis:** the review names the assertion
  `result == result` as tautological and explains it can never fail,
  regardless of what `divide` actually returns.
- **R2 - proportionate scope:** the review does not invent unrelated
  requirements or block on anything besides the planted defect.
