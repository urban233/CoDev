# Task

Update `slug.py` so that `slugify(value)` returns a lowercase slug with leading
and trailing whitespace removed and every run of whitespace replaced by one
hyphen. Preserve existing hyphens. Keep the change bounded to `slug.py`; do not
edit the tests or add dependencies.
