# Intentionally empty: CoDev uses bzlmod (see MODULE.bazel) for dependency
# management. This file exists only so tooling that still looks for a
# WORKSPACE marker (e.g. the vscode-bazel extension, see
# https://github.com/bazelbuild/vscode-bazel/issues/314) can find the
# workspace root.
